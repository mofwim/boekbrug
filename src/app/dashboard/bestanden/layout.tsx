// src/app/dashboard/bestanden/layout.tsx
// [BOEK-028] Bestanden has its own Drive-style header — exclude DashboardHeader — May 2026

export default function BestandenLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}